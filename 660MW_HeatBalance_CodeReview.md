# 🔥 660MW-Heat-Balance 项目源码审查报告

> 审查人：资深火电热动工程师 + 软件架构师视角
> 审查范围：后端核心代码（models / solvers / properties / templates / api）
> 审查方式：GitHub 公开源码静态分析

---

## 📊 问题总览

| 等级 | 数量 | 含义 |
|------|------|------|
| 🔴 **致命** | 1 | 核心算法错误，结果完全不可信 |
| 🟠 **严重** | 7 | 工程逻辑错误，导致关键指标偏差 |
| 🟡 **一般** | 6 | 参数或实现细节不匹配工程实际 |
| 🟢 **轻微** | 4 | 代码风格、可维护性问题 |

---

## 🔴 致命级问题（1项）

### 1. 汽轮机等熵膨胀计算根本性错误

**位置**：`turbine.py` `_calculate_isentropic_enthalpy()`

**问题**：用饱和点线性插值计算过热区等熵焓降，方法完全错误。

```python
# 错误代码（第175行附近）
h_out_is = h_g + (s_in - s_g) * (h_g - h_f) / max(s_g - s_f, 1e-10)
```

**为什么错**：
- 等熵膨胀是 **s = 常数** 的过程，需要在出口压力下查 **相同熵值** 对应的焓
- 代码用 `(h_g-h_f)/(s_g-s_f)` 当斜率，这只在饱和线上成立
- 过热蒸汽的等熵线不是直线，不能拿饱和点插值
- 25MPa/600°C → 4.9kPa 的膨胀，这样算出来的焓降可能偏差 **10% 以上**

**后果**：汽轮机功率、抽汽参数、整机效率、煤耗——全部不准。

**修复**：强制用 IAPWS-IF97 库，在出口压力下按 `s = s_in` 求解：
```python
sub = IAPWS97(P=p_out, s=s_in)
h_out_is = sub.h
```

---

## 🟠 严重级问题（7项）

### 2. 抽汽焓计算用压力比例近似 ❌

**位置**：`turbine.py` `_calculate_traditional()`

```python
frac = (p_in - ext_p) / (p_in - p_out)
h_ext = h_in - frac * eta_isen * (h_in - h_out_is)
```

**问题**：用压力比例代替焓降比例，超临界区非线性极强，误差巨大。且 `eta_isen` 不应直接乘在抽汽焓上。

**修复**：在抽汽压力 `ext_p` 下，按 `s = s_in` 做等熵膨胀求 `h_ext_is`，再用 `h_ext = h_in - eta_isen*(h_in-h_ext_is)`。

---

### 3. 多级分段模式焓降分配错误 ❌

**位置**：`turbine.py` `_calculate_multistage()`

```python
h_drop_actual = (h_drop_ratio/total_ratio) * (h_in - h_out_is) * eta_isen
```

**问题**：总焓降按比例分配再统一乘效率。实际每段效率不同（HP 88%、IP 90%、LP 88%），且每段等熵焓降应基于该段入口参数重新计算。

---

### 4. 高加上端差设为 -1.7°C ❌

**位置**：`heater.py` 默认参数 & `plant_600mw.py`

**问题**：表面式加热器出口水温不可能超过蒸汽饱和温度。负端差在物理上不可能。

**修复**：改为正值（2.5~5.5°C），或显式建模过热度利用。

---

### 5. 除氧器热平衡方程写错 ❌

**位置**：`heater.py` `_calculate_da()`

```python
# 错误：除以了 eta
m_steam*h_steam + ... = (m_total) * h_water_out / eta
```

**问题**：除氧器是混合式，没有传热端差，效率应 ≈100%。除以 `eta` 导致蒸汽量被高估。

**修复**：
```python
m_steam*h_steam + m_drain_in*h_drain_in + m_water*h_water_in = m_total * h_f
```

---

### 6. 疏水闪蒸未处理 ❌

**位置**：`plant_600mw.py` 连接关系

**问题**：LP5（1.5MPa）疏水流入 LP6（0.6MPa），压差 0.9MPa，疏水会闪蒸。代码中直接把高压疏水焓当低压疏水焓用，未考虑闪蒸放热。

---

### 7. 求解器收敛逻辑可能死循环 ❌

**位置**：`heat_balance.py` `_check_convergence()`

**问题**：功率偏差判据 < 0.1%，但抽汽量迭代若震荡，功率跟着震荡，可能永远不满足。缺少松弛因子。

---

### 8. 抽汽量更新用经验系数 0.3 ❌

**位置**：`heat_balance.py` `_update_extraction_shares()`

```python
target_ext_m = self.main_steam_flow * ratio * 0.3  # 拍脑袋系数
```

**问题**：抽汽量应由加热器热平衡 **精确求解**，而非经验估算。这会导致收敛慢或假收敛。

---

## 🟡 一般级问题（6项）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| 9 | 锅炉未考虑排污 | `boiler.py` | 给水 ≠ 主汽流量，差约1%排污 |
| 10 | 凝汽器端差概念误用 | `condenser.py` | TTD 是排汽与冷却水出口温差，代码未用于冷却水侧计算 |
| 11 | 给水泵功耗未扣除 | `heat_balance.py` | 600MW机组给水泵约15~20MW，应从功率中扣 |
| 12 | 发电机连接关系错误 | `plant_600mw.py` | 三个缸排汽口都连发电机，应为串联后统一输入 |
| 13 | 超临界 fallback 公式误差大 | `steam.py` | 25MPa/600°C 用线性近似，偏差可能 >5% |
| 14 | 模板参数与典型值偏差 | `plant_600mw.py` | 主汽25MPa偏低，给水275°C偏低，HP1抽汽7.2MPa偏高 |

---

## 🟢 轻微级问题（4项）

| # | 问题 | 说明 |
|---|------|------|
| 15 | 错误处理静默吞异常 | 大量 `try/except: pass`，问题被隐藏 |
| 16 | 类型注解不精确 | `Dict[str, Any]` 泛滥 |
| 17 | 魔法数字硬编码 | 效率值、经验系数应提取为配置 |
| 18 | 缺少单元测试 | 无法验证单个设备模型正确性 |

---

## 🎯 总体评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 工程概念 | ⭐⭐⭐⭐ | 双层迭代、模块化设计思路正确 |
| 计算精度 | ⭐ | 核心算法错误，结果不可信 |
| 代码结构 | ⭐⭐⭐ | 清晰但细节粗糙 |
| 工程可用 | ⭐ | 当前不可用于实际计算 |

---

## ✅ 优先修复清单（按顺序）

1. **重写 `turbine.py` 等熵膨胀** → 用 IAPWS-IF97 的 `ps_to_h`
2. **修正 `heater.py` 除氧器热平衡** → 去掉 `/eta`
3. **修正高加上端差** → 改为正值 2.5~5.5°C
4. **修正抽汽焓计算** → 每级按 `s=const` 做等熵膨胀
5. **重写抽汽量更新** → 由加热器热平衡精确反推
6. **补充给水泵汽轮机功耗扣除**
7. **修正发电机连接关系** → 串联汇总后输入
8. **编写单元测试** → 对标热力设计手册验证单设备

---

> 💡 一句话总结：**框架搭得不错，但热动核心的膨胀计算和热平衡方程写错了，拿这个算煤耗会差出几十克，上不了工程。**
